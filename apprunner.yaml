version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install
      - pip install -r requirements.txt
run:
  command: python main.py
  network:
    port: 8000