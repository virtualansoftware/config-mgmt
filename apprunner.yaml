version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3 -m pip install
      - pip install pipenv
      - pip install -r requirements.txt
run:
  command: python main.py
  network:
    port: 8000