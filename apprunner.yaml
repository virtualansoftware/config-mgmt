version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install pipenv
      - pipenv install
      - pip3 install -r requirements.txt
run:
  command: python3 main.py
  network:
    port: 8000